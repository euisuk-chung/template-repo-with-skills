## 변경 내용

<!-- 무엇을 왜 바꿨는지 요약합니다. -->

## 확인

- [ ] `uv run ruff check .` / `uv run ruff format --check .`
- [ ] `uv run pytest`
- [ ] `.agents/skills`를 바꿨다면 `python3 scripts/sync_skills.py`를 실행하고 생성물을 함께 커밋함
