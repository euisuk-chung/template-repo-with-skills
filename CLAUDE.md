@AGENTS.md

# Claude Code

Skills under `.claude/skills` are generated text adapters. When an adapter is
selected, read the complete canonical `.agents/skills/<skill-name>/SKILL.md` named in
its body (path relative to the repository root) and follow that canonical file.
Resolve relative resource links from the canonical skill directory.
